/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["topshare.vn", "static.247phim.com", "img.netpop.app"],
  },
};

module.exports = nextConfig;
